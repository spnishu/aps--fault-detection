### Step 1 - Install the requirements

```bash
pip install -r requirements.txt
```

### Step 2 - Run main.py file

```bash
python main.py

Git commands
If you are starting a project and want to use git in your project

git init

This is going to initialize git in your source code.

or

You can clone existing github repo

git clone<github url>`

Note:Clone/ Download github repo in your system`

Add your changes made in file to git stagging area

git add file_name

Note: You can give file name to add specific file or use dot to add everything to staging area `

Create commits
.............

git commit -m "message"

....
git push origin main

Note: origin --> contains url to your github repo
main--> is your branch name.

To push your changes forcefully
..
git push origin main -f


To pull changes from github repo

git pull origin main